export enum BookStatus {
  NONE = '',
  HOT = 'HOT',
  NEW = 'NEW',
}
export interface IIAMUser {
  id: string;
  // username: string;
  // emailVerified: boolean;
  // name?: string;
  // givenName?: string;
  // familyName?: string;
  // email?: string;
}
